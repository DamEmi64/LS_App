<Window x:Class="Translations.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Translations"
        mc:Ignorable="d"
        Title="Translations" Height="350" Width="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="300*"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>
        <TabControl Grid.ColumnSpan="2" Margin="0,0,0,39" Grid.RowSpan="2">
            <TabItem Header="Dictionaries">
                <DataGrid ItemsSource="{Binding Dictionaries}" />
            </TabItem>
            <TabItem Header="Translations">
                <DataGrid ItemsSource="{Binding Translations}" />
            </TabItem>
        </TabControl>
        <Button Content="Load" Grid.Row="1" Command="{Binding Load}"/>
        <Button Content="Generate" Grid.Row="1" Grid.Column="1" Command="{Binding Generate}"/>
        
    </Grid>
</Window>
